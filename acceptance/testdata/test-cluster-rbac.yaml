apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: "acceptance-test-clusterrole"
rules:
  - apiGroups:
      - "*"
    resources:
      - configmaps
      - serviceaccounts
    verbs:
      - "*"
