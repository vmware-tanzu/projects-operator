platform: linux

image_resource:
  type: registry-image
  source:
    repository: ismteam/ci
    tag: 0.0.8

inputs:
- name: marketplace-project

run:
  path: marketplace-project/ci/shim
  args:
     - make
     - unit-tests
